/**
 * Xnor gate:
 * if (a = b) out = 1, else out = 0 
 */
CHIP Xnor {
    IN a, b;
    OUT out;
    
    PARTS:
    Xor(a=a, b=b, out=axorb);
    Nand(a=a, b=b, out=anandb);
    Nand(a=axorb, b=anandb, out=out);
}   